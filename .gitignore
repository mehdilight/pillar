/vendor
/.phpunit.cache
/public/editor
node_modules

# Build artefacts, at any depth — examples/starter has its own.
dist/
.pillar/

# Plugin dashboard bundles are built with the dashboard (npm run build in apps/editor).
plugins/*/editor/dist/

.DS_Store
