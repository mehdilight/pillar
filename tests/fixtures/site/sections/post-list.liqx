---
const entries = paginate ? paginate.items : collections.posts.items;
---
<ul class="post-list">
  {entries.map((post) => <PostCard post={post} />)}
</ul>
<schema>
  { "name": "Post list", "settings": [] }
</schema>
